# DomainArena Build Notes — 2026-08-24

## Session summary

### Commits this session
| Commit | Content |
| --- | --- |
| `4c6fe30` | Implementation pack zip added to repo |
| `pre-domainarena-hackathon-2026-08-24` | Baseline tag (lineage anchor) |
| `1f83d4e` | Product backend: name.com feasibility, semantic inversion, AB/BA arena, audience-conditioned Pareto policy, HTTP + MCP APIs |

### name.com API status
- **Docs imported**: `docs/namecom-api/` — full Core API doc set from
  docs.name.com/llms.txt: **105 markdown pages** (reference + guides + MCP).
  Key pages: `api__v1__reference__domains__search.md`,
  `...check-availability.md`, `...create-domain.md`, `guides__domain-pricing.md`.
- **Sandbox credentials DEAD**: token 403s on api.dev.name.com (account appears
  unprovisioned). Do not rely on sandbox until fixed.
- **Production credentials LIVE** (read-only usage): search verified against
  api.name.com `/core/v1/domains:search` — results match our
  InventorySnapshot parser exactly.
- **Safety guard**: `register_domain()` raises unless mode == "sandbox".
  Current mode in `runner/.env` is `production-readonly`.

### Built this session
1. `docs/EXPERIMENT_REDESIGN.md` — canonical v2 protocols derived from the
   evidence ledger + peer review (within-run contrasts, position-stratified
   estimands, degraded-description regime, injective verifiers).
2. `domainarena/fixtures.py` — hardcoded canonical prior results
   (H-ASL001a/b, H-ASL002C, H-TLD01 corrected estimands, H-NAMING01 ceiling null).
3. `domainarena/arena/execution.py` — CP5 execution-grounded selection env:
   separate SELECTED→INVOKED→VALID_PARAMS→TASK_VERIFIED funnel, hidden
   deterministic verifier, errors never counted as selections.
4. `domainarena/arena/stability.py` — CP6 per-family × serving-window matrix,
   worst-family reporting, stability gate (≥2 windows agree, n≥30).
5. `domainarena/arena/tld.py` — TLD causal productization with
   position-stratified primary endpoints (P(pick|pos0), P(pick|off-pos0)).
6. `domainarena/generators.py` — heterogeneous candidate families +
   inventory intersection.
7. `scripts/run_demo_pipeline.py` — offline end-to-end pipeline demo
   (verified running: intent → feasibility → recommendation → execution funnel → robustness).

### Known issues / next steps
- [ ] Sandbox account needs provisioning at name.com before registration/DNS demos.
- [ ] Live candidate generation ∩ production inventory (search-backed generator).
- [ ] Wire real CF Workers AI backends into DA-T4 discovery trials.
- [ ] Web UI (six screens per pack 11_UI_UX.md).
- [ ] Legacy lab test failures predate DomainArena (missing `sentinel` module).

### Credentials policy
All tokens live in `runner/.env` (gitignored, chmod 600). Never commit.
